apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-inference-gateway.fullname" . }}
  labels:
    {{- include "ai-inference-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-inference-gateway.selectorLabels" . | nindent 4 }}
